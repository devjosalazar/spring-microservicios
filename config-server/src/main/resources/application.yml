server:
  port: 7777

spring:
  profiles:
    active: default
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devjosalazar/microservice-config.git
          default-label: main
          clone-on-start: true
          username: josalazar@uniquindio.edu.co
          password: dev1094905479


eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka